@model IEnumerable<SIMPEDA_V01.Models.Shelter>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.lokasiShelter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.deskripsiShelter)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lokasiShelter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.deskripsiShelter)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idShelter }) |
                @Html.ActionLink("Details", "Details", new { id=item.idShelter }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idShelter })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
